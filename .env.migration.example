# Keep real values in .env.migration (which must not be committed).
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Optional: default is 3. Keep this low to limit memory and network use.
MIGRATION_CONCURRENCY=3
